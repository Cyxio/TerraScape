using Terraria;
using Terraria.ID;
using Terraria.ModLoader;

namespace OldSchoolRuneScape.Items.Magic
{
    /*public class Bind : ModItem
    {
        public override void SetStaticDefaults()
        {
            DisplayName.SetDefault("Bind");
            Tooltip.SetDefault("Casts a bolt that slows the enemy for one second");
        }
        public override void SetDefaults()
        {
            item.mana = 20;
            item.width = 40;
            item.height = 40;
            item.useTime = 60;
            item.useAnimation = 60;
            item.useStyle = 5;
            item.noUseGraphic = true;
            item.noMelee = true;
            item.rare = 1;
            item.UseSound = SoundID.Item20;
            item.autoReuse = false;
            item.shoot = ModContent.ProjectileType<Projectiles.Bind>();
            item.shootSpeed = 12f;
        }
        public override void AddRecipes()
        {
            ModRecipe recipe = new ModRecipe(mod);
            recipe.AddIngredient(null, "Earthrune", 15);
            recipe.AddIngredient(null, "Waterrune", 15);
            recipe.AddIngredient(null, "Naturerune", 10);
            recipe.SetResult(this);
            recipe.AddTile(TileID.WorkBenches);
            recipe.AddRecipe();
        }
    }*/
}